services:
  - type: web
    name: travel-app
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run backend/app.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
